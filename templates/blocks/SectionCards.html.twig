{# "slots" is the nested-Block relation, not part of Block::$data, so it is read straight off "block" #}
<twig:c975LUi:Section:Cards eyebrow="{{ eyebrow|default('') }}" title="{{ title|default('') }}" slots="{{ block.slots }}" id="{{ anchor_id }}"/>
