{# "content" was already escaped by EmailTemplateRenderer::contentToHtml(); "text-left" beats the theme's "td{text-align:center}" #}
<tr>
    <td class="text-left" style="padding:0 24px 16px;font-size:15px;line-height:22px;">
        {{ content|raw }}
    </td>
</tr>
