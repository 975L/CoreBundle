{# Donovan's dashboard card; DonovanWidgetProvider already gated it, so there is no "not ready" state to branch on #}
<h2>{{ ai_assistant_name() }}</h2>
{{ include('@c975LUi/management/_ai_assistant_widget.html.twig') }}
