{# Adapter for the dynamic Block system — maps Block data to the Progress:Tracker component #}
<twig:c975LUi:Progress:Tracker eyebrow="{{ eyebrow|default('') }}" title="{{ title|default('') }}" total="{{ total|default(1) }}" completed="{{ completed|default(0) }}" note="{{ note|default('') }}"/>
