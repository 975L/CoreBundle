{# Adapter for the dynamic Block system — maps Block data to the Feature:Bar component #}
<twig:c975LUi:Feature:Bar eyebrow="{{ eyebrow|default('') }}" title="{{ title|default('') }}" items="{{ items|default([]) }}" background="{{ background|default('') }}" id="{{ anchor_id }}"/>
