{# Adapter for the dynamic Block system — maps Block data to the Map:Map component #}
<twig:c975LUi:Map:Map title="{{ title|default('') }}" points="{{ points|default([]) }}" height="{{ height|default('medium') }}" zoom="{{ zoom|default(13) }}" id="{{ anchor_id }}"/>
