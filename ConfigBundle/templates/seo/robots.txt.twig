# https://www.robotstxt.org/
# Generated by c975l:seo:files:create, edit the "seo" configs from the back-office rather than this file: the next run overwrites it

User-agent: *
Allow: /
{% for path in disallow %}
Disallow: {{ path }}
{% endfor %}
{% if blockAi and aiCrawlers %}

# AI crawlers that harvest pages to train models. Advisory only: this is honoured
# by the operators listed here, nothing enforces it. Answer engines (Claude-User,
# OAI-SearchBot, PerplexityBot, ChatGPT-User...) are left out on purpose: they fetch
# a page to answer someone's question and cite it back, so blocking them only costs
# visibility.
{% for crawler in aiCrawlers %}
User-agent: {{ crawler }}
{% endfor %}
Disallow: /
{% endif %}
{% if extra %}

{{ extra }}
{% endif %}
{% if sitemap %}

Sitemap: {{ sitemap }}
{% endif %}
